import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

class ecomWithApi extends  StatefulWidget{

  State<ecomWithApi> createState()=> stateEcomWithApi();
}

class stateEcomWithApi extends State<ecomWithApi>
{
@override
  Widget build(BuildContext context) {
   return Scaffold(
    appBar: AppBar(title: Text("Ecomerce"),),

    body: Container(
      child: Column(
        children: [],
      ),
    ),
   );
  }
}